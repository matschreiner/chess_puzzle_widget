<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/tab_difficulty"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center"
            android:padding="16dp"
            android:textSize="15sp"
            android:textStyle="bold"
            android:textColor="@color/status_text_color"
            android:text="@string/tab_difficulty" />

        <TextView
            android:id="@+id/tab_theme"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center"
            android:padding="16dp"
            android:textSize="15sp"
            android:textStyle="bold"
            android:textColor="@color/status_text_color"
            android:text="@string/tab_theme" />

    </LinearLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <LinearLayout
                android:id="@+id/difficulty_page"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="8dp" />

            <LinearLayout
                android:id="@+id/theme_page"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="8dp"
                android:visibility="gone" />

        </FrameLayout>

    </ScrollView>

    <Button
        android:id="@+id/ok_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:paddingVertical="12dp"
        android:background="@drawable/ok_button_background"
        app:backgroundTint="@null"
        app:rippleColor="@color/board_light_square"
        android:stateListAnimator="@null"
        android:textColor="@color/status_text_color"
        android:textAllCaps="false"
        android:textSize="16sp"
        android:elevation="0dp"
        android:text="@string/ok_button" />

</LinearLayout>
